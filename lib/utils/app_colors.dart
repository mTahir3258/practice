import 'dart:ui';

class AppColors {
  // Primary colors based on screen reference (Dark Slate/Green)
  static const Color primaryDark = Color(0xFF1E3A39); // Dark background color
  static const Color primaryGreen = Color(
    0xFF5ED35F,
  ); // Primary CTA/Accent color
  // static const Color primary = Color(0xFFC4ECCB);

  static const Color textLight = Color(
    0xFFF0FDF4,
  ); // Light text for dark background
  static const Color textDark = Color(
    0xFF1E3A39,
  ); // Dark text for light background

  // Secondary/Support Colors
  static const Color greyBackground = Color(
    0xFF273F3D,
  ); // Slightly lighter grey for fields/cards
  static const Color errorRed = Color(0xFFE53935);
}
